before_script:
  - export DEBIAN_FRONTEND=noninteractive
  - apt-get update && apt-get install -y --no-install-recommends apt-utils
  - dpkg --add-architecture i386
  - apt-get update -qq && apt-get install -y -qq --no-install-recommends apt-utils lib32z1 lib32ncurses5
  - /builds/stereotech/stefirmware/smoothieware/linux_install
  - /builds/stereotech/stefirmware/smoothieware/BuildShell


stages:
  - build

run-build:
  stage: build
  script:
    - make clean all