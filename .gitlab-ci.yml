before_script:
 - /builds/stereotech/stefirmware/smoothieware/linux_install
 - apt-get update -qq && apt-get install -y -force-yes libc6-dev-i386 build-essential
 - /builds/stereotech/stefirmware/smoothieware/BuildShell

stages:
  - build

run-build:
  stage: build
  script:
    - make clean all